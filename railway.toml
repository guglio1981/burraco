[build]
builder = "nixpacks"
buildCommand = "npm install && npm install -g tsx"

[deploy]
startCommand = "tsx apps/server/src/index.ts"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[environments.production.variables]
NODE_ENV = "production"
